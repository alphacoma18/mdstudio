import { useContext } from "react";
import ContextEditor from "@/utils/context/editor";
import GenForm from "@/components/gen/form";
const EditorFileSetting: React.FC = () => {
    const {} = useContext(ContextEditor);
    return <GenForm props={{
        
    }}></GenForm>;
};

export default EditorFileSetting;
